# Example: Insert data into PostgreSQL from CSV or CLI
# Adjust your connection config inside imp_postgresql.py

from import.imp_postgresql import *

# Usage: script loads data and inserts into the target table defined inside imp_postgresql.py
